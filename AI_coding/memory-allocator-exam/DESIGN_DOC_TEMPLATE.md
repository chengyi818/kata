# 设计文档

> 候选人请填写以下各节。这是评估的重要部分。

---

## 一、基础版本架构

### 1.1 数据结构设计

请描述你的`block_header_t`和`allocator_t`结构体设计（各字段含义）。

```
（在此填写）
```

### 1.2 核心算法

简述my_malloc（First-Fit + Split）和my_free（Coalesce）的流程。

```
（在此填写）
```

### 1.3 碎片统计

说明你如何计算以下指标：
- 总空闲字节数
- 外部碎片率（external fragmentation ratio）
- 最大连续空闲块

```
（在此填写）
```

---

## 二、SOLID原则分析（基础版本）

分析基础版本中各SOLID原则的遵守/违反情况。

### 2.1 单一职责原则（SRP）
> 基础版本的allocator是否有多个职责？如果有，分别是什么？

```
（在此填写）
```

### 2.2 开闭原则（OCP）
> 如果要在基础版本中添加Best-Fit策略，需要修改哪些代码？为什么这违反了OCP？

```
（在此填写）
```

### 2.3 依赖倒置原则（DIP）
> 基础版本中的allocator是否依赖于具体实现？如果是，依赖了什么？

```
（在此填写）
```

### 2.4 接口隔离原则（ISP）
> 基础版本中是否有"胖接口"问题？

```
（在此填写）
```

### 2.5 里氏替换原则（LSP）
> 如果基础版本中有一个"策略"的概念，不同的策略能否互相替换？

```
（在此填写）
```

---

## 三、扩展版本架构

### 3.1 架构图

请画出扩展版本的组件关系图（可以用ASCII art）。

```
（在此填写，例如：）

┌──────────────────────┐
│    allocator_t       │
│  ┌────────────────┐  │
│  │  strategy*     │──┼─────── alloc_strategy_t (接口)
│  │  pool          │  │           ▲
│  │  free_list     │  │    ┌──────┴──────┐
│  └────────────────┘  │    │             │
└──────────────────────┘  FF   BF   ...   Buddy
```

### 3.2 策略接口设计

你的`alloc_strategy_t`接口定义及各方法职责。

```
（在此填写）
```

### 3.3 工厂模式

策略工厂的实现方式（枚举类型、创建/销毁方法）。

```
（在此填写）
```

---

## 四、SOLID原则修复说明

对于第二部分中分析的每个违反，说明扩展版本如何修复。

| 原则 | 基础版本问题 | 扩展版本修复方式 |
|------|-------------|-----------------|
| SRP  |             |                 |
| OCP  |             |                 |
| DIP  |             |                 |
| ISP  |             |                 |
| LSP  |             |                 |

---

## 五、Buddy System简化设计

### 5.1 简化策略

完整Buddy System使用按order分组的空闲链表。你的简化版本做了哪些简化？

```
（在此填写）
```

### 5.2 2的幂次取整

描述你对请求大小进行2的幂次取整的方法。

```
（在此填写）
```

### 5.3 Buddy合并

描述释放时buddy合并的逻辑（如何找到buddy，合并条件）。

```
（在此填写）
```

---

## 六、设计权衡

### 6.1 策略模式的开销

策略模式带来了什么运行时或代码层面的开销？

```
（在此填写）
```

### 6.2 简化Buddy vs 完整Buddy

你的简化版Buddy System与完整实现相比，在性能和碎片方面有何差异？

```
（在此填写）
```

### 6.3 扩展性验证

如果要添加一种新策略（如Next-Fit），需要新增/修改哪些文件？哪些现有文件完全不需要修改？

```
（在此填写）
```
