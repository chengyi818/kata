# 设计文档模板

> **说明**：请填写以下各节。第一部分只需填写「基础部分」相关节；第二部分补充「扩展部分」相关节。方括号 `[ ]` 中的内容为填写提示。

---

## 一、基础部分设计

### 1.1 架构概述

[用文字或ASCII图描述你的整体架构。包括：模拟器主控逻辑与页面置换算法的关系、数据流向。]

```
[请在此处绘制架构图，例如：

+------------------+
|   main()         |
|   - 解析输入     |
|   - 驱动模拟     |
+--------+---------+
         |
         v
+--------+---------+
|   模拟器核心     |
|   - 遍历序列     |
|   - 判断缺页     |
|   - 统计结果     |
+--------+---------+
         |
         v
+--------+---------+
|   置换算法模块   |
|   - 查找页面     |
|   - 选择换出页   |
+------------------+
]
```

### 1.2 数据结构设计

[列出你使用的核心数据结构，说明每个字段的含义和用途。]

| 结构体/类型 | 字段 | 用途 |
|-------------|------|------|
| `[名称]` | `[字段名]: [类型]` | `[说明]` |
| | | |

### 1.3 算法流程

[以流程图或伪代码描述你实现的页面置换算法的关键步骤。重点说明：命中判断逻辑、缺页时的换出选择逻辑。]

```
[伪代码或步骤描述]

1. 遍历访问序列中的每个页号 page:
2.   在物理页框中查找 page 是否存在
3.   如果找到:
4.      命中，不计数
5.   如果未找到:
6.      缺页计数 +1
7.      如果物理页框未满:
8.          将 page 放入空闲页框
9.      否则:
10.         根据置换算法选择一个页框
11.         换出旧页面，换入新页面
12. 输出统计结果
```

### 1.4 复杂度分析

- 每次访问的时间复杂度：[O(?)]  分析理由：[ ]
- 空间复杂度：[O(?)]  分析理由：[ ]

### 1.5 设计决策与权衡

[回答以下问题，每个问题2-3句即可]

1. **为什么选择FIFO/LRU算法？**  
   [ ]

2. **如果未来需要新增算法（如Clock），当前代码需要改动哪些地方？预估改动量？**  
   [ ]

3. **当前设计最大的不足是什么？**  
   [ ]

---

## 二、扩展部分设计

### 2.1 重构后的架构

[绘制扩展后的架构图，重点突出接口抽象层和算法实现层。标注使用的设计模式。]

```
[架构图]
```

### 2.2 设计模式应用

1. **使用的设计模式**：[如：策略模式 + 简单工厂模式]

2. **模式在本项目中的角色映射**：

   | 模式角色 | 项目中的对应 | 说明 |
   |----------|-------------|------|
   | Strategy（策略接口） | `[接口名/结构体名]` | [说明] |
   | ConcreteStrategy（具体策略） | `[每个算法文件]` | [说明] |
   | Context（上下文） | `[模拟器核心]` | [说明] |
   | Factory（工厂） | `[工厂函数名]` | [说明] |

### 2.3 SOLID原则分析

**开闭原则（OCP）：**

- 扩展部分是否做到了"对扩展开放，对修改关闭"？
- 如果新增一个算法（如LFU），需要修改哪些已有文件？需要新建哪些文件？
- [请逐文件列出]

**依赖反转原则（DIP）：**

- 模拟器核心依赖的是具体算法还是抽象接口？
- 抽象接口由谁定义（高层模块还是低层模块）？
- [请说明]

### 2.4 重构过程

[描述你从基础版本到扩展版本的重构步骤：]

1. **抽取接口**：如何定义统一算法接口？  
   [ ]

2. **改造旧代码**：原有的FIFO/LRU如何适配新接口？  
   [ ]

3. **添加工厂**：如何实现运行时算法选择？  
   [ ]

4. **新增算法**：新增Clock/Optimal算法时的具体步骤？  
   [ ]

### 2.5 新算法设计

| 算法名 | 核心数据结构 | 换出策略简述 |
|--------|-------------|-------------|
| Clock | [ ] | [ ] |
| Optimal | [ ] | [ ] |
| LFU（如实现） | [ ] | [ ] |

---

## 三、测试策略

### 3.1 测试用例设计

| 测试场景 | 输入 | 预期输出 | 测试目的 |
|----------|------|----------|----------|
| 全部命中 | 序列:[1,1,1,1], 帧数:1 | 缺页1次 | 边界：首次加载后的命中 |
| 全部缺页 | 序列:[1,2,3,4,5], 帧数:3 | [ ] | 验证换出逻辑 |
| Belady异常序列 | 序列:[1,2,3,4,1,2,5,1,2,3,4,5], 帧数:3和4 | [ ] | 验证FIFO的Belady异常 |
| 空序列 | 序列:[], 帧数:4 | 缺页0次 | 边界 |
| 单页框 | 序列:[1,2,1,2], 帧数:1 | [ ] | 边界 |
| 已知标准结果 | [使用课程中的标准序列] | FIFO=15, LRU=12, Optimal=9 | 正确性基准 |

### 3.2 测试环境

- 编译器：[ ]
- 编译选项：[ ]
- 运行方式：[ ]

---

## 四、自我评估

| 评估项 | 完成度 | 备注 |
|--------|--------|------|
| 基础功能正确 | □ 是 □ 否 | |
| 扩展功能正确 | □ 是 □ 否 | |
| OCP（新增算法不改旧代码） | □ 是 □ 否 | |
| DIP（依赖抽象不依赖具体） | □ 是 □ 否 | |
| 无内存泄漏 | □ 是 □ 否 | |
| 测试通过 | □ 是 □ 否 | |
