# 设计报告：Linux Runqueue 任务队列管理器

> **候选人姓名**：_______________
> **完成时间**：_______________
> **使用语言**：_______________

---

## 一、问题分析：硬编码的代价

你在基础部分 `rq_pick_next()` 中实现了硬编码的严格优先级调度。

### 1.1 请逐条列出硬编码实现存在的问题

```
示例格式:
  问题1: _______________________________
  影响: _______________________________

  问题2: _______________________________
  影响: _______________________________

  （请至少列出3个问题）
```

>（在此作答）







### 1.2 变更影响分析

假设产品经理提出以下需求变更，请估算在**基础实现（硬编码）**中分别需要修改哪些文件、哪些函数：

| 需求变更 | 需修改的文件 | 需修改的函数 | 预计改动行数 |
|----------|-------------|-------------|-------------|
| 新增"轮转调度"策略（同优先级时间片轮转） | | | |
| 新增"最早截止时间优先(EDF)"策略 | | | |
| 入队前增加"CPU配额检查" | | | |
| 入队前增加"内存限制检查" | | | |
| 同时启用CPU配额检查 + 内存限制检查 | | | |

---

## 二、设计模式选择与论证

### 2.1 为什么选择策略模式

```
问题: 策略模式如何解决"添加新调度策略需修改核心代码"的问题？
请结合你的代码实现说明。
```

>（在此作答）







### 2.2 为什么选择装饰器模式

```
问题: 装饰器模式如何实现"多个过滤器可任意组合"而不修改已有代码？
请结合你的代码实现说明。责任链模式在这里起到了什么作用？
```

>（在此作答）







### 2.3 备选方案分析

```
如果不用策略模式+装饰器模式，还有哪些替代方案？
请评估至少2种替代方案，并说明为什么不选用。
```

| 替代方案 | 优点 | 缺点 | 不选用的理由 |
|----------|------|------|-------------|
| | | | |
| | | | |

---

## 三、SOLID 原则体现

### 3.1 单一职责原则（SRP）

```
基础实现中 runqueue_t 承当了哪些职责？
扩展实现中如何将这些职责分离到独立的模块？
```

>（在此作答）






### 3.2 开闭原则（OCP）

```
扩展实现中，如何做到"对扩展开放，对修改关闭"？
举一个具体例子：如果要新增"随机调度策略"，需要做哪些事？
```

>（在此作答）






### 3.3 依赖反转原则（DIP）

```
基础实现中 rq_pick_next() 的依赖关系是怎样的？
扩展实现中如何反转了这个依赖？
```

>（在此作答：画出基础实现的依赖箭头 vs 扩展实现的依赖箭头）

```
基础实现:   runqueue ──依赖──▶ 具体调度逻辑（硬编码）

扩展实现:   runqueue ──依赖──▶ pick_strategy_t (抽象接口)
                                ▲
                                │ 实现
                     ┌──────────┼──────────┐
              strict_priority   cfs    load_balance
```




---

## 四、架构对比

### 4.1 可维护性量化对比

| 维度 | 基础实现（硬编码） | 扩展实现（设计模式） | 改进说明 |
|------|-------------------|---------------------|----------|
| 新增调度策略需修改几个文件 | | | |
| 新增过滤器需修改几个函数 | | | |
| 单元测试的独立性 | | | |
| 生产环境切换策略的难度 | | | |
| 代码圈复杂度（大致估算） | | | |

### 4.2 性能影响

```
引入策略模式和装饰器模式后，性能是否有影响？
（如虚函数调用开销、内存分配开销等）
在你的场景下，这个开销是否可以接受？为什么？
```

>（在此作答）





---

## 五、AI 辅助开发记录

> **如实填写**：本部分考察候选人对AI工具的理解和批判性思维能力。
> 使用了AI工具 → 请如实说明；未使用 → 填写"未使用"并简述独立开发过程。

### 5.1 AI 使用概况

| 项目 | 内容 |
|------|------|
| 使用的AI工具 | |
| 基础部分：AI辅助了哪些工作 | |
| 扩展部分：AI辅助了哪些工作 | |
| 设计报告：AI辅助了哪些工作 | |

### 5.2 AI 输出的正确性与局限性

```
请列举 AI 工具在本次开发过程中的表现：
  1. AI 做得好的地方（给出具体例子）:
  2. AI 出现错误或不足的地方（给出具体例子）:
  3. 你是如何发现和修正 AI 错误的？
  4. 如果不用 AI，你会在哪些地方花更多时间？
```

>（在此作答）









### 5.3 AI 与架构设计

```
AI 工具是否倾向于直接给出"最优"架构方案（如直接使用策略模式），
而不是先让你写硬编码再重构？

你认为这种"先硬编码再重构"的题目设计，对考察真实的架构能力是否有意义？
```

>（在此作答）





---

## 六、自我评价

### 6.1 完成度自评

| 功能点 | 完成？ | 备注 |
|--------|--------|------|
| 基础 enqueue/dequeue | ✅ / ❌ | |
| 基础 pick_next（严格优先级） | ✅ / ❌ | |
| 基础 change_priority | ✅ / ❌ | |
| 基础测试全部通过 | ✅ / ❌ | |
| 策略模式：strict_priority | ✅ / ❌ | |
| 策略模式：cfs | ✅ / ❌ | |
| 策略模式：load_balance（加分） | ✅ / ❌ | |
| 装饰器模式：cgroup_cpu | ✅ / ❌ | |
| 装饰器模式：cgroup_mems | ✅ / ❌ | |
| 过滤器链 filter_chain | ✅ / ❌ | |
| rq_pick_with_strategy | ✅ / ❌ | |
| rq_apply_filter | ✅ / ❌ | |

### 6.2 遇到的困难与收获

```
困难:

收获:
```

>（在此作答）





---

**报告完成。谢谢！**
